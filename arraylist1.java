import java.util.*;

public class arraylist1 {
    //Write a program that reads a file and displays the words as a list
    ArrayList<String> file = new ArrayList<String>;
    Scanner console = new Scanner(new File("words.txt"));

    while(console.hasNext())
}
